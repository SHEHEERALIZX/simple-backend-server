# version: "3"
# services:
#  app:
#  container_name: app
#  image: Dockerfile
#  restart: always
#  build: .
#  ports:
#  - "3000:3000"
#  links:
#  - mongo
#  mongo:
#  container_name: mongo
#  image: mongo
#  volumes:
#  - ./data:/data/db
#  ports:
#  - '27017:27017'

version: '3.9'
services:
   mongo:
    container_name: mongodb_database
    image: mongo:latest
    volumes:
      - mongo_dedicated_volume:/data/db

   api_server:
    container_name: myapp
    build: .
    ports:
      - 3000:3000    

    environment:
     URI: mongo://mongodb_database/test


volumes:
  mongo_dedicated_volume: